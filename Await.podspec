Pod::Spec.new do |s|
  s.name         = "Await"
  s.version      = "0.0.1"
  s.summary      = "A short description of Await."
  s.license      = { :type => "MIT", :file => "LICENSE" }
  s.author       = { "dean.w" => "dean.w@kakaocorp.com" }
  s.source_files = "Await/*.swift"
end
